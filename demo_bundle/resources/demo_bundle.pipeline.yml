resources:
  pipelines:
    demo_bundle_pipeline:
      name: demo_bundle_pipeline
      schema: pipeline_bundle_${bundle.target}
      libraries:
        - notebook:
            path: /Workspace/Users/meghana.tripathi@simbustech.com/asset_bundle_demo_task/demo_bundle/src/notebook
      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
